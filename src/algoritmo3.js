//Item c
//Algoritmo 3

const numbers = [1,2,3,4,5];   //Cria uma constante "numbers" que armazena uma lista que vai de 1 a 5
const isOdd = (x) =>  return x % 2; // cria uma constante "isOdd" que recebe x e usa uma arrow function que retornaria x % 2 que seria o restante da divisão
}
const output = arr.filter(isOdd); //cria uma constante "output" e usa o arr.filter que recebe como parametro o "isOdd "para filtrar o "isOdd"
console.log(output); // imprime o resultado da constante "output" já com os resultados "filtrados"
