//Item d
//Algoritmo 4.js


const arr = [1,2,3,4,5]  // cria a constante "arr" que contém uma lista de números que vai de 1 a 5
const output = arr.filter((x)=>  x > 4); //cria uma constante output que que usa o filter no "arr", os numeros da lista vai ficar dentro da variavel x, e o filter vai filtrar os numeros que são maiores que 4
console.log(output); //vai imprimir o resultado já filtrado pelo filter da constante output com os numeros maiores que 4
